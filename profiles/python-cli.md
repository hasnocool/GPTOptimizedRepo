# Profile: Python CLI

Apply: PYTHON, ARCHITECTURE, DEPENDENCIES, SECURITY, PERFORMANCE, TESTING, CONFIGURATION, DOCUMENTATION, RELEASES, GIT_AND_PRS, REPOSITORY_HYGIENE, CLI, LICENSING_IP, and AGENT_BEHAVIOR.

Additional requirements:
- stable exit codes and useful `--help`;
- stdout for normal output and stderr for diagnostics;
- structured output mode for automation when useful;
- explicit confirmation for destructive commands;
- deterministic noninteractive mode for scripts/CI;
- documented config/env/flag precedence.
